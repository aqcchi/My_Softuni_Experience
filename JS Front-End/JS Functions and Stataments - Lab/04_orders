function finalSum(product, quantity) {

    const itemPrices = {
        coffee: 1.50,
        water: 1.00,
        coke: 1.40,
        snacks: 2.00
    };

    total = itemPrices[product] * quantity

    console.log(total.toFixed(2))
}

finalSum("water", 5)
finalSum("coffee", 2)
