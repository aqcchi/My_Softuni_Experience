function settingToObject(firstName, lastName, age) {

    const personInfo = {
        firstName,
        lastName,
        age,
    }

    return personInfo
}

console.log(settingToObject("Peter", "Pan", "20"))
